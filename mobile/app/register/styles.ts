// styles.ts
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'red'
    },
    formLogin: {
        display: 'flex',
        gap: 15
    },
});

export default styles;
